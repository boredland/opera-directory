# Implementation Plans

Generated by the `improve` skill on 2026-06-18 (against commit `5cdc985`).
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done.

Theme: the product is the committed JSON graph under `data/`, produced by ~197
adapters with **zero tests** and **no CI quality gate**. Plans 001–004 make that
product *guarded and refactor-safe*; 005–006 are cheap data-hygiene wins; 007–008
are observability and de-duplication.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Issue | Status |
|------|-------|----------|--------|------------|-------|--------|
| 001 | Fix lint + format baseline; migrate Biome config | P1 | S | — | [#55](https://github.com/boredland/opera-directory/issues/55) | DONE (merged `379fb7a`; +`.gitignore .claude/` `c4f5635`) |
| 002 | `validate` command — referential integrity + sanity | P1 | M | — | [#48](https://github.com/boredland/opera-directory/issues/48) | DONE (worktree branch `worktree-agent-a349f030f248a5bee`, commit `de36eae`, pending merge) |
| 003 | CI quality gate (typecheck/lint on push; validate before commit) | P1 | S | 001, 002 | [#49](https://github.com/boredland/opera-directory/issues/49) | DONE (merged `8b187aa`; ci.yml fired on push) |
| 004 | Characterization tests for resolver + store | P1 | M | — | [#50](https://github.com/boredland/opera-directory/issues/50) | DONE (merged `9bf4d23`, 24 tests; CI now runs them) |
| 005 | `prune` command for orphan global entities | P2 | M | 002 | [#51](https://github.com/boredland/opera-directory/issues/51) | DONE (merged; ~505 orphans removed, validate 0). ⚠ executor auto-pushed past review (content verified OK). FOLLOW-UP: prune is single-pass — pruning works cascades to orphan their composers, so it needs running to fixpoint (done manually here); make `prune` loop internally. |
| 006 | Staatsoper Hamburg: stop minting dates as people | P2 | S | — | [#52](https://github.com/boredland/opera-directory/issues/52) | DONE (merged `2af4389`; live scrape clean) |
| 007 | Per-house scrape health signal (observability) | P2 | M | — | [#53](https://github.com/boredland/opera-directory/issues/53) | DONE (merged `25fa9dc`, 7 tests) |
| 008 | Shared date-parsing helpers + pilot migration | P3 | L | 004 | [#54](https://github.com/boredland/opera-directory/issues/54) | DONE (merged `bf3d275`; `_dates.ts` +16 tests; 4 adapters migrated, byte-identical). FOLLOW-UP: roll `isoFromParts` out to the ~45 other `padStart` adapters; consolidate credit-label maps. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended sequence

1. **001** then **002** — both unblock **003** (a green lint gate and a `validate`
   command). 002 and 004 can proceed in parallel.
2. **003** — turns the gate on. After this, every push runs typecheck/lint and
   every scrape validates the graph before committing.
3. **004** — the safety net before any resolver change; do early.
4. **006** (cheap bug fix) → then **005** (`prune` clears the orphans 006 leaves
   behind, and any others). Run 005's `--apply` only after reviewing its dry-run.
5. **007** — observability; independent, do when convenient.
6. **008** — last; it's a spike + pilot, and wants 004's test harness in place.

## Dependency notes

- **003 requires 001 and 002**: it wires `bun run lint` (must be green) and
  `bun run validate` (must exist) into CI.
- **005 builds on 002**: same reference-collection traversal; and uses
  `bun run validate` to prove no dangling refs after a prune.
- **008 requires 004**: the new shared helpers must ship with unit tests, which
  need the `bun test` harness 004 sets up.
- **006 + 005 interplay**: fix Hamburg (006), re-scrape it, then prune (005) to
  delete the now-orphaned date-named persons. Independent to start, sequenced to finish.

## Findings considered and rejected / deferred

- **Person name-collapse corruption** (distinct people sharing a name slug;
  name-variant split): architecturally real and documented as the known Phase-1
  Wikidata/MusicBrainz resolution work (`resolve.ts:33-36` TODO), but **0 measured
  cases in current data**. It is a *feature* (Tier-1 resolution), not a present
  bug — out of scope for these guard/hygiene plans. Land 004's tests first.
- **Security**: no high-confidence findings. Minor hardening only — SHA-pin GitHub
  Actions (currently tag-pinned), render `page.goto` redirect allowlisting,
  `house_slug`→path is fed only by the curated registry so traversal isn't
  reachable. Not worth dedicated plans now.
- **Dependencies**: healthy (`bun audit` clean, no major-version lag, no duplicate
  libs). Nothing to do.

## Direction (product options, not bugs — for the maintainer)

- **Wikidata Tier-1 + MusicBrainz Tier-2 resolution** (README §4; `resolve.ts`
  TODO): the stubbed accuracy lever; QID-keyed dedup fixes the name-collapse class
  wholesale. Large, network-dependent, needs a review queue. Highest-value feature
  work — do it after 004.
- **The web app (Phase 3)**: the public payoff is entirely unbuilt; the data layer
  is ready to serve it. Wants a query-shape design pass first.
- **Derived SQLite/D1 read-copy** (README §6): only once the app exists and
  `performances` (12.5k now) outgrows array-loading. Premature today.
