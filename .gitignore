node_modules/
dist/
.turbo/
*.log

# Local agent state + transient git worktrees (would otherwise pollute `biome check .`)
.claude/

# Local canonical store + scrape caches (regenerated by the pipeline)
*.sqlite
*.sqlite-*
data/cache/
# Per-house raw scrape output (CI artifact intermediates; ingest merges them)
raw/

.env
.env.*
!.env.example
.DS_Store
